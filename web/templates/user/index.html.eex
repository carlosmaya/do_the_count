<h1> Listing users </h1>

<table class="table">
  <%= for user <- @users do %>
    <tr>
      <td>
        <b><%= user.name %></b> ( <%= user.email %> )
      </td>
      <td><%= link "view", to: user_path(@conn, :show, user.id) %></td>
    </tr>
  <% end %>
</table>
